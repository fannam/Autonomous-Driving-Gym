# Legacy Packages

This directory holds older code that remains runnable but is not the main development target.

- `AlphaZero-based-autonomous-driving/`
  Original single-agent AlphaZero training, inference, and self-play workflows.

New shared infrastructure and multi-agent work should go under `source/`. Keep changes here focused on compatibility, bug fixes, and preserving existing workflows.
